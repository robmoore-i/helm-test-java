apiVersion: v1
kind: Secret
metadata:
  name: app-password
data:
  {{ if (.Values.equalityTesting).useRandomSecret }}
  password: {{ randAlphaNum 32 | quote }}
  {{ else }}
  # 'password123'
  password: cGFzc3dvcmQxMjM=
  {{ end }}
